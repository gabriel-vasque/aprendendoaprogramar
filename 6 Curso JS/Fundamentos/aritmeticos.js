const [ a, b, c, d ] = [ 3, 5, 1, 15 ]


// d++   : post fix
// --d   : pre fix
// a + b : fix
// -a    : opera sobre um único operando

const soma = a + b+ c + d
const subtracao = d - b
const multiplicacao = a * b
const divisao = d / a
const modulo = a % 2

console.log(soma, subtracao, multiplicacao, divisao, modulo)